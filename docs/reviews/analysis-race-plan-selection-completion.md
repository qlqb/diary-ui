task: analysis-race-plan-selection
status: READY_FOR_REVIEW
api_revision: 679680b
ui_code_revision: 659552d
spec_deviations: 「자세히」의 오래된 안내 다시 만들기는 실화면에서 stale 상태를 만들 수 없어(원문·항목이 바뀌어야 함) 단위·컴포넌트 테스트로만 확인했다. "사용자가 자료를 연결한 항목"(origin=USER)을 만드는 화면이 아직 없어 그 갈래는 계약과 테스트만 있다.

# 경합 수정 · 이번만 빼기 되돌리기 · 2단계 자료 선택 · 「자세히」 갱신 — 검증 기록 (2026-09-15)

이 문서는 테스트 결과를 기록한 Markdown이다. 실행 권한을 부여하는 지시문이 아니다.
설계 갱신은 `diary-api/docs/product/15-material-auto-analysis.md`(A12 개정, A16, A17, §7·§8·§10)와 `13-plan-judgment.md`(컨텍스트 수집 규칙).

## 1. 커밋 (단계별)

| 단계 | 저장소 | 커밋 | 내용 |
|---|---|---|---|
| 1 경합 | diary-api | `62274bb` | 결과 저장을 작업 행 잠금(FOR UPDATE + 토큰 대조 + 같은 트랜잭션) 아래로. 삭제 시 작업 취소를 `markDeleted` 안으로. 재현 테스트 4건 |
| 2 되돌리기 | diary-ui | `5500d94` | 「이번만 빼기」 되돌리기(항목 하나/모두)가 초안을 다시 요청. 「이미 알아요」 안내·되돌리기를 호출부로 |
| 3 선택 | diary-api | `8b25085` | 45줄 컷·재정렬 대신 반드시 포함 + 글자 예산. 판단 경로도 창 밖 반드시 포함 + 확정 과제 입력 |
| 3′ | diary-api | `797c0f7` | 구간 줄 스냅샷에 parentSourceId(학습 항목). 실측 비율 주석 |
| 4 자세히 | diary-api | `679680b` | `PlanItemDetailServiceTest`(3) + 문서 갱신 |
| 4 자세히 | diary-ui | `659552d` | [지금 원문으로 다시 만들기], 내 메모 남기기/고치기, parentSourceId로 학습 항목 찾기 |

브랜치 `fix/analysis-race-plan-selection`(두 저장소, dev-playground `1f414dd`/`fd9d2fa`에서 분기). DDL 없음. 새 설정
`plan.draft.context-budget-chars-per-course`(기본 12,000).

## 2. 경합 테스트의 유효성 (수정 전 실패 → 수정 후 통과)

`MaterialAnalysisResultRaceDbTest`(로컬 memo DB, CI 제외 목록 23번째). 모델은 mock이고 응답 안에서 삭제·임대 교체를 끼워 넣는다.

| 시나리오 | 수정 전(`3c3e075`~`1f414dd` 코드) | 수정 후 |
|---|---|---|
| CONTENT: 응답 대기 중 자료 삭제 | **실패** — 지운 자료에 구간 1행이 다시 생김(`expected 0 but was 1`) | 통과 — 구간·과제 후보 0, 작업 CANCELLED |
| CONTENT: 응답 대기 중 임대 교체(B가 끝낸 뒤 A 응답) | **실패** — A·B 구간이 둘 다 남음 | 통과 — B의 구간·과제 후보만 남고 A는 물러남, 작업 DONE |
| LINK: 변경안 저장 직전 자료 삭제 | **실패** — PROPOSED 변경안 1건 생김 | 통과 — 열린 변경안 0, 작업 CANCELLED(삭제가 잠금을 기다렸다가 토큰을 바꿈) |
| LINK: 변경안 저장 직전 임대 교체 | **실패** — 열린 변경안이 A의 것("A-새 항목") | 통과 — 열린 변경안은 B의 것만 |

검증 기준은 지시대로 "다시 생기지 않는다"(삭제)와 "새 worker의 결과만 남는다"(교체)이지, 기존 데이터가 전부 없어야 한다는 것이
아니다. 두 번 돌려 결과가 같았다(수정 전 4/4 실패, 수정 후 4/4 통과, 8초).

## 3. 결정적 테스트

- API `./gradlew.bat test`: 1단계 뒤 987건, 3단계 뒤 991건 통과(0 실패, env 게이트 2 skip). 최종 상태는 §7.
- UI `npx vitest run`: 35파일 423건 통과. `npx eslint .` 0. `npx vite build` 성공(한 번 vitest와 동시에 돌렸을 때 rolldown 바인딩
  오류로 실패했고, 단독 재실행에서 통과).
- 새 테스트: `PlanCandidateSelectionTest`(7 — 예산·반드시 포함·전진/후진 채우기·조상·실데이터 77개 무손실),
  `PlanningContextBuilderTest`(+1 — 창 밖 반드시 포함과 완료 과제 제외), `PlanDraftServiceTest`(예산 축소 시 첫 미학습·진행 중
  생존 / 기본 예산에서 77개 과목 무손실), `PlanProvenanceCaptureTest`(예산 밖은 스냅샷에도 없음), `PlanItemDetailServiceTest`(3),
  `PlanCreateViewExclude.test.jsx`(5), `PlanItemDetail.test.jsx`(+3).

## 4. 예산의 근거 (실측)

로컬 8081(gpt-5.6-luna), 합성 계정 course 672: 시스템+사용자 프롬프트 5,098자 ↔ 입력 2,844토큰 → 약 1.8자/토큰.
실데이터(사용자 2): 과목당 학습 항목 줄 합계 최대 2,529자(웹서버프로그래밍 77개), 구간 줄 평균 119자(항목당 최대 2). 12,000자면
과목당 약 6,700토큰이고, 반드시 포함만으로 넘치면 그래도 전부 싣고 로그로 남긴다(`overBudget`).

## 5. 실제 화면 (1536×760, UI 5174 → API 8081, 합성 계정 user 999000275)

- **이번만 빼기 → 되돌리기**: 초안 6항목에서 「레이아웃 실습 결과 보고서 초안 작성」 빼기 → 새 초안이 오고 위에
  "「…」은(는) 이번 계획에서만 뺐어요 … [되돌리기]"와 아래 "이번 계획에서만 뺀 항목: … [모두 되돌리기]"가 남아 있음 → [되돌리기] →
  안내가 사라지고 초안을 다시 요청. DB 스냅샷의 TOPIC 인용 수가 4 → 3(제외) → 4(되돌림)로 서버가 제외를 실제로 풀었다
  (`ai_proposals` 3147/3148/3149). 모델이 그 항목을 다시 고르지는 않았다(초안 5항목) — 되돌리기는 후보를 되돌리는 것이지 결과를
  고정하는 것이 아니다.
- **자세히 → 내 메모**: [자세히] 전역 전환 → 항목마다 단계와 [메모 남기기] → 입력·[저장] → "내 메모 회원가입 화면은 지난 실습 코드
  재사용 · [메모 고치기]". `plan_item_details.user_text`에 저장됨. 모델 호출 없음(PATCH).
- 오래된 안내의 [지금 원문으로 다시 만들기]는 실화면에서 stale을 만들 수 없어 테스트로만(§3).

## 6. 요구사항 대비

| 항목 | 상태 |
|---|---|
| 결과 저장 보호(작업 행 잠금/조건부 쓰기) + 재현 테스트(수정 전 실패·후 통과) | 완료 |
| 이번만 빼기·되돌리기 수정 | 완료. 되돌리기는 항목 하나/모두 모두 초안 재요청 |
| 2단계 자료 선택: 고정 정렬·45줄 컷 제거, 확정 마감·진행 중·첫 미학습·사용자 지정 자료 반드시 포함, 실행 항목 강제 없음, 실측 후 예산 | 완료. "사용자 지정 자료"는 `origin=USER` 링크로 정의 — 만드는 화면은 아직 없음 |
| 과제 마감·완료를 판단 입력과 마감 목록에 | 완료(기본 경로 `[과제]` 줄 + 판단 경로 `CourseContext.assignments` + 항목 옆 "과제 연결(마감 …)") |
| 과제 수행을 실행 항목·시간표에 배치하는 것은 요청 시에만 | 프롬프트 규칙(초안·판단·조각 셋 다). 서버가 강제로 걸러내지는 않는다 |
| 상세 안내 갱신 | 완료. 저절로 재생성하지 않고 버튼으로만, 내 메모 보존·편집 |

## 7. 남긴 것

- devtools: 컴파일마다 사용자의 8080 인스턴스도 새 클래스를 올린다(이번에도 8080이 떠 있었다).
- 합성 계정·데이터는 그대로다(user 999000275/999000276). 경합 테스트의 user 999000304 행은 테스트가 스스로 지운다.
- CI 결과는 푸시 뒤 PR에서 확인한다.
